export const updateClassname = (classname) => {
  return {
    type: 'UPDATE_CLASSNAME',
    classname
  }
}

export const updateLevel = (level) => {
  return {
    type: 'UPDATE_LEVEL',
    level
  }
}
